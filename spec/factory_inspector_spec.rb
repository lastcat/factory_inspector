require 'spec_helper'

describe FactoryInspector do
  it 'has a version number' do
    expect(FactoryInspector::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
